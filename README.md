# ThisTimer releases

Public repository for ThisTimer release artifacts.

- Desktop installers are mirrored here by CI from the private source repository.
- `gh-pages` is used to host `update-policy.json` and the custom domain content.
- Do not edit release artifacts manually; they are published by automation.
